<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppDisertatieVacantaAI.Pages.RegisterNamePage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource DarkGray}}">
    
    <ScrollView>
        <VerticalStackLayout Padding="30,50" Spacing="24">
            
            <!-- Progress Indicator -->
            <Grid Margin="0,10,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <!-- Step 1 - Active -->
                <BoxView Grid.Column="0" BackgroundColor="{StaticResource PrimaryBlue}" 
                         HeightRequest="4" CornerRadius="2" Margin="0,0,5,0"/>
                <!-- Step 2 - Inactive -->
                <BoxView Grid.Column="1" BackgroundColor="{AppThemeBinding Light={StaticResource LightGray}, Dark={StaticResource MediumGray}}" 
                         HeightRequest="4" CornerRadius="2" Margin="5,0"/>
                <!-- Step 3 - Inactive -->
                <BoxView Grid.Column="2" BackgroundColor="{AppThemeBinding Light={StaticResource LightGray}, Dark={StaticResource MediumGray}}" 
                         HeightRequest="4" CornerRadius="2" Margin="5,0"/>
                <!-- Step 4 - Inactive -->
                <BoxView Grid.Column="3" BackgroundColor="{AppThemeBinding Light={StaticResource LightGray}, Dark={StaticResource MediumGray}}" 
                         HeightRequest="4" CornerRadius="2" Margin="5,0,0,0"/>
            </Grid>
            
            <!-- Progress Text -->
            <Label Text="Pasul 1 din 4" 
                   FontSize="14"
                   TextColor="{AppThemeBinding Light={StaticResource MediumGray}, Dark={StaticResource LightGray}}"
                   HorizontalOptions="Center"
                   Margin="0,0,0,10"/>

            <!-- Header Section -->
            <VerticalStackLayout Spacing="8">
                <Label Text="Bun venit! 👋" 
                       FontSize="28" 
                       FontAttributes="Bold" 
                       TextColor="{AppThemeBinding Light={StaticResource DarkGray}, Dark={StaticResource LightGray}}" 
                       HorizontalOptions="Center"/>
                <Label Text="Sa incepem cu numele tau pentru a-ti personaliza experienta" 
                       FontSize="16"
                       TextColor="{AppThemeBinding Light={StaticResource MediumGray}, Dark={StaticResource LightGray}}" 
                       Opacity="0.8" 
                       HorizontalOptions="Center"
                       HorizontalTextAlignment="Center"/>
            </VerticalStackLayout>

            <!-- Input Fields -->
            <VerticalStackLayout Spacing="16" Margin="0,20,0,0">
                <!-- First Name -->
                <VerticalStackLayout Spacing="8">
                    <Label Text="Numele de familie" 
                           FontSize="14" 
                           FontAttributes="Bold"
                           TextColor="{AppThemeBinding Light={StaticResource DarkGray}, Dark={StaticResource LightGray}}"/>
                    <Frame BackgroundColor="{AppThemeBinding Light={StaticResource LightGray}, Dark={StaticResource MediumGray}}" 
                           CornerRadius="12" 
                           Padding="0" 
                           HasShadow="False"
                           x:Name="NumeFrame">
                        <Entry x:Name="NumeEntry" 
                               Placeholder="ex. Popescu" 
                               PlaceholderColor="{AppThemeBinding Light={StaticResource MediumGray}, Dark={StaticResource LightGray}}" 
                               TextColor="{AppThemeBinding Light={StaticResource DarkGray}, Dark={StaticResource LightGray}}"
                               BackgroundColor="Transparent"
                               Margin="16,12"
                               TextChanged="OnNumeTextChanged"
                               ReturnType="Next"
                               ReturnCommand="{Binding FocusPrenumeCommand}"/>
                    </Frame>
                </VerticalStackLayout>

                <!-- Last Name -->
                <VerticalStackLayout Spacing="8">
                    <Label Text="Prenumele" 
                           FontSize="14" 
                           FontAttributes="Bold"
                           TextColor="{AppThemeBinding Light={StaticResource DarkGray}, Dark={StaticResource LightGray}}"/>
                    <Frame BackgroundColor="{AppThemeBinding Light={StaticResource LightGray}, Dark={StaticResource MediumGray}}" 
                           CornerRadius="12" 
                           Padding="0" 
                           HasShadow="False"
                           x:Name="PrenumeFrame">
                        <Entry x:Name="PrenumeEntry" 
                               Placeholder="ex. Maria" 
                               PlaceholderColor="{AppThemeBinding Light={StaticResource MediumGray}, Dark={StaticResource LightGray}}" 
                               TextColor="{AppThemeBinding Light={StaticResource DarkGray}, Dark={StaticResource LightGray}}"
                               BackgroundColor="Transparent"
                               Margin="16,12"
                               TextChanged="OnPrenumeTextChanged"
                               ReturnType="Done"/>
                    </Frame>
                </VerticalStackLayout>
            </VerticalStackLayout>

            <!-- Error Message -->
            <Label x:Name="ErrorLabel" 
                   TextColor="#FF6B6B" 
                   IsVisible="False" 
                   FontSize="14"
                   Margin="4,0,0,0"/>

            <!-- Action Buttons -->
            <VerticalStackLayout Spacing="12" Margin="0,20,0,0">
                <Button x:Name="ContinueButton"
                        Text="Continua" 
                        Clicked="OnNext" 
                        BackgroundColor="{StaticResource PrimaryBlue}" 
                        TextColor="White"
                        CornerRadius="12"
                        HeightRequest="50"
                        FontSize="16"
                        FontAttributes="Bold"
                        IsEnabled="False"/>
                
                <Button Text="Renunta" 
                        Clicked="OnCancel" 
                        BackgroundColor="Transparent" 
                        TextColor="{AppThemeBinding Light={StaticResource MediumGray}, Dark={StaticResource LightGray}}"
                        FontSize="15"/>
            </VerticalStackLayout>

            <!-- Tips Section -->
            <Frame BackgroundColor="{AppThemeBinding Light=#F8F9FA, Dark={StaticResource MediumGray}}" 
                   CornerRadius="12" 
                   Padding="16" 
                   HasShadow="False"
                   Margin="0,10,0,0">
                <HorizontalStackLayout Spacing="12">
                    <Label Text="💡" FontSize="18" VerticalOptions="Start" Margin="0,2,0,0"/>
                    <Label Text="Foloseste numele real pentru o experienta personalizata optima" 
                           FontSize="13"
                           TextColor="{AppThemeBinding Light={StaticResource MediumGray}, Dark={StaticResource LightGray}}"
                           VerticalOptions="Center"
                           LineBreakMode="WordWrap"/>
                </HorizontalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>