<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppDisertatieVacantaAI.Pages.SettingsPage"
             Title="Settings"
             Shell.NavBarIsVisible="True"
             BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource DarkGray}}">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="24">
            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto" >
                <Label Text="Dark Mode" VerticalOptions="Center" TextColor="{AppThemeBinding Light={StaticResource DarkGray}, Dark={StaticResource LightGray}}" />
                <Switch x:Name="ThemeSwitch" Grid.Column="1" Toggled="OnThemeToggled" />
            </Grid>
            <BoxView HeightRequest="2" BackgroundColor="{AppThemeBinding Light={StaticResource LightGray}, Dark={StaticResource MediumGray}}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>