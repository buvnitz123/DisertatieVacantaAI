<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppDisertatieVacantaAI.Pages.ChatConversationPage"
             Title="Chat"
             Shell.NavBarIsVisible="True"
             BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource DarkGray}}">
    
    <Grid RowDefinitions="*,Auto">
        <!-- Messages -->
        <CollectionView x:Name="MessagesView"
                        Grid.Row="0"
                        ItemsSource="{Binding}"
                        SelectionMode="None"
                        VerticalScrollBarVisibility="Never"
                        Margin="10,10,10,0"
                        ItemSizingStrategy="MeasureAllItems">
            
            <!-- Header for Load More button -->
            <CollectionView.Header>
                <Grid x:Name="LoadMoreHeader" Padding="0,10">
                    <Button x:Name="LoadMoreButton"
                            Text="⬆️ Încarcă mai multe mesaje"
                            BackgroundColor="Transparent"
                            TextColor="{StaticResource PrimaryBlue}"
                            BorderColor="{StaticResource PrimaryBlue}"
                            BorderWidth="1"
                            CornerRadius="20"
                            HeightRequest="40"
                            FontSize="14"
                            HorizontalOptions="Center"
                            Clicked="OnLoadMoreClicked"
                            IsVisible="False" />
                    
                    <ActivityIndicator x:Name="LoadingMoreIndicator"
                                       IsRunning="False"
                                       Color="{StaticResource PrimaryBlue}"
                                       HeightRequest="30"
                                       HorizontalOptions="Center"
                                       IsVisible="False" />
                </Grid>
            </CollectionView.Header>
            
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="4">
                        <Grid.Resources>
                            <Style TargetType="Frame" x:Key="BubbleStyle">
                                <Setter Property="CornerRadius" Value="16" />
                                <Setter Property="Padding" Value="12,8" />
                                <Setter Property="HasShadow" Value="False" />
                            </Style>
                        </Grid.Resources>
                        <VerticalStackLayout HorizontalOptions="{Binding HorizontalAlignment}">
                            <Frame Style="{StaticResource BubbleStyle}"
                                   BackgroundColor="{Binding BubbleColor}"
                                   MaximumWidthRequest="280">
                                <VerticalStackLayout Spacing="4">
                                    <Label FormattedText="{Binding FormattedText}"
                                           TextColor="White"
                                           LineBreakMode="WordWrap" />

                                    <Button Text="{Binding ActionButtonText}"
                                            IsVisible="{Binding HasAction}"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=ActionCommand}"
                                            CommandParameter="{Binding .}"
                                            BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource DarkGray}}"
                                            TextColor="{AppThemeBinding Light={StaticResource PrimaryBlue}, Dark={StaticResource White}}"
                                            CornerRadius="10"
                                            Margin="0,5,0,0"
                                            HeightRequest="36"
                                            FontSize="13"
                                            FontAttributes="Bold"
                                            Padding="10,0"/>

                                    <Label Text="{Binding TimeString}"
                                           FontSize="10"
                                           HorizontalOptions="End"
                                           TextColor="#CCCCCC" />
                                </VerticalStackLayout>
                            </Frame>

                            <!-- Rating - sub balon -->
                            <VerticalStackLayout IsVisible="{Binding ShowRating}"
                                                 Margin="4,6,0,0"
                                                 Spacing="4">
                                <Label Text="Vă place răspunsul?"
                                       FontSize="11"
                                       TextColor="{AppThemeBinding Light={StaticResource MediumGray}, Dark={StaticResource LightGray}}"
                                       Opacity="0.8" />
                                <HorizontalStackLayout Spacing="16">
                                    <Label Text="👍"
                                           FontSize="20">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=RateLikeCommand}"
                                                                  CommandParameter="{Binding .}" />
                                        </Label.GestureRecognizers>
                                    </Label>
                                    <Label Text="👎"
                                           FontSize="20">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=RateDislikeCommand}"
                                                                  CommandParameter="{Binding .}" />
                                        </Label.GestureRecognizers>
                                    </Label>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>

                            <!-- Feedback după rating -->
                            <Label Text="{Binding RatingText}"
                                   IsVisible="{Binding HasRatingText}"
                                   FontSize="11"
                                   Margin="4,4,0,0"
                                   TextColor="{AppThemeBinding Light={StaticResource MediumGray}, Dark={StaticResource LightGray}}"
                                   Opacity="0.7" />
                        </VerticalStackLayout>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Input Bar -->
        <Grid Grid.Row="1" BackgroundColor="{AppThemeBinding Light={StaticResource LightGray}, Dark={StaticResource MediumGray}}" Padding="12,10" ColumnDefinitions="*,Auto" RowDefinitions="Auto" >
            <Entry x:Name="MessageEntry" 
                   Placeholder="Scrie un mesaj..." 
                   PlaceholderColor="{AppThemeBinding Light={StaticResource MediumGray}, Dark={StaticResource LightGray}}" 
                   TextColor="{AppThemeBinding Light={StaticResource DarkGray}, Dark={StaticResource White}}" 
                   BackgroundColor="Transparent" 
                   Completed="OnSendClicked"
                   ReturnType="Send" />
            <Button Text="➤" 
                    Grid.Column="1" 
                    x:Name="SendButton"
                    Clicked="OnSendClicked" 
                    BackgroundColor="{StaticResource PrimaryBlue}" 
                    TextColor="White" 
                    CornerRadius="18" 
                    WidthRequest="48" 
                    HeightRequest="48" />
        </Grid>
    </Grid>
</ContentPage>